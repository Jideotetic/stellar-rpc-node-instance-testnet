services:
  rpc1:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config:/opt/stellar
      - ./data:/opt/stellar/data
    restart: always

  rpc2:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./config:/opt/stellar
      - ./data:/opt/stellar/data
    restart: always

  rpc3:
    build: .
    ports:
      - "8002:8000"
    volumes:
      - ./config:/opt/stellar
      - ./data:/opt/stellar/data
    restart: always
